type: object
required:
  - nameIdentifier
properties:
  nameIdentifier:
    description: "Name identifier for the person."
    type: array
    items:
      $ref: ./PersonNameIdentifier.yaml
xml:
  name: PersonName
