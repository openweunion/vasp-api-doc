type: object
properties:
  id:
    $ref: ./TransactionId.yaml
  originatorVASPId:
    $ref: ./VASPId.yaml
  beneficiaryVASPId:
    $ref: ./VASPId.yaml
  originatorCustomerId:
    type: string
    $ref: ./CustomerId.yaml
  travelRuleData:
    description: "Travel Rule data for the transaction."
    type: object
    $ref: ./TravelRuleData.yaml
  withdrawalInfo:
    description: "Withdrawal information for the transaction."
    type: object
    $ref: ./WithdrawalInfo.yaml
  quote:
    description: "Quote information for the transaction."
    $ref: OffRampQuote.yaml
  status:
    description: "Status of the transaction."
    type: string
    enum:
      - "NEW"
      - "CANCELLED"
      - "APPROVED"
      - "ACCEPTED"
      - "REJECTED"
      - "PENDING_TRANSFER"
      - "TRANSFERRED"
      - "COMPLETED"
  referenceId:
    description: "Bank or payment provider reference ID for withdrawal."
    type: [string, "null"]
    example: null
  transactionHash:
    description: "The hash of the transaction."
    type: [string, "null"]
    example: null
  createdTime:
    description: "The time when the transaction was created (ISO 8601 format)."
    type: string
    format: date-time
    example: "2025-08-07T09:00:00Z"
  updatedTime:
    description: "The time when the transaction was last updated (ISO 8601 format)."
    type: string
    format: date-time
    example: "2025-08-07T10:30:00Z"
xml:
  name: OffRampTransaction
